import React from "react";


function CreateHeader ({name}) {
    return(
        <div>
            <header>
                <h1>{name}</h1>
            </header>
        </div>
    )
}

export default CreateHeader;